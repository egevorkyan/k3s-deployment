all:
  children:
    initial:
      hosts:
  {% for items in infodata %}
      {{ items.host_name }}:
          ansible_user: pilot
          ansible_host: {{ items.host_ip }}
          ansible_python_interpreter: /usr/bin/python3
          server_id: {{ loop.index }}
  {% endfor %}